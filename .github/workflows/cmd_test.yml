name: Test Broken Workflow

on:
  push:
    branches:
      - main
      - workflow-error-test   # run on both branches
  workflow_dispatch:           # manual run button in GitHub Actions UI

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
      uses: actions/checkout@v4

      # ❌ Intentional error (wrong command)
      - name: Create folder
        run: mkdri test_folder

      # ❌ Intentional error (typo in --version)
      - name: Check Node version
        run: node --verison
